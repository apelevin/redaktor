Отлично, понял задачу 👍
Ниже — **чистое, каноническое описание рекомендуемого подхода**, без вариантов “можно так / можно иначе”, без сравнений, без обсуждений.
Это текст, который ты **просто копируешь в Cursor** как концептуальный раздел архитектуры.

---

```md
# Рекомендуемый подход: адаптивная генерация вопросов через схему документа

## 1. Базовый принцип

Юридический агент **не должен задавать вопросы напрямую из общего списка**.  
Вместо этого он работает в два этапа:

1. **Сначала строит внутреннюю схему документа (Document Plan)** на основании миссии и уже имеющегося контента.
2. **Затем генерирует вопросы строго из этой схемы**, запрашивая только недостающие или неопределённые данные.

Таким образом:
- вопросы всегда релевантны конкретному договору,
- отсутствуют повторяющиеся и нерелевантные вопросы,
- порядок вопросов соответствует логике документа.

---

## 2. Этап 1 — Генерация схемы документа (Document Plan)

### 2.1. Назначение схемы

`DocumentPlan` — это внутренняя карта документа, которая описывает:
- структуру будущего документа,
- какие данные обязательно нужно получить от пользователя,
- какие юридические решения требуют явного выбора,
- какие опциональные пакеты условий возможны.

Схема **не является текстом договора** и **не показывается пользователю**.  
Она используется исключительно для управления пайплайном и HITL.

---

### 2.2. Генерация схемы

Схема генерируется **один раз или пересобирается при существенных изменениях** (миссия, уровень рассуждения, существенные пользовательские правки).

Вход для генерации:
- миссия документа (цель, юрисдикция, роли сторон),
- выбранный уровень рассуждения (`basic | standard | professional`),
- текущая версия документа (если есть).

Выход:
- структурированная схема документа (Document Plan).

Генерация схемы может выполняться с помощью LLM, но:
- схема **обязана соответствовать заранее заданной структуре данных**,
- после генерации схема проходит **детерминированную валидацию**.

---

## 3. Структура Document Plan

### 3.1. Структурное дерево документа

Схема описывает будущий документ как дерево:

- разделы,
- пункты,
- приложения (при необходимости).

Каждый узел:
- имеет стабильный `id`,
- связан с будущим местом в документе,
- используется для подсветки в UI.

---

### 3.2. Обязательные поля (Required Fields)

`RequiredField` описывает **конкретное значение**, без которого невозможно корректно сформировать документ.

Примеры:
- адрес объекта аренды,
- срок договора,
- размер платы,
- реквизиты стороны,
- кадастровый номер.

Каждое поле:
- имеет тип (дата, деньги, текст, форма стороны),
- связано с конкретным узлом схемы,
- имеет приоритет важности,
- имеет статус: `missing / present / uncertain`.

---

### 3.3. Юридические решения (Decision Points)

`DecisionPoint` описывает **юридическую развилку**, где допустимо несколько формулировок.

Примеры:
- наличие депозита,
- лимит ответственности,
- порядок расторжения,
- модель передачи прав,
- способ разрешения споров.

Каждое решение:
- имеет ключ (для сохранения выбора),
- набор допустимых вариантов,
- описание юридического эффекта,
- уровень риска,
- признак обязательности.

---

### 3.4. Опциональные пакеты

Схема может содержать **опциональные пакеты условий**, которые:
- не обязательны,
- но могут быть рекомендованы при определённом контексте или уровне рассуждения.

Примеры:
- расширенные гарантии,
- дополнительные акты,
- специальные условия ответственности.

---

## 4. Этап 2 — Генерация вопросов из схемы

### 4.1. Принцип формирования вопросов

Вопросы **не генерируются “из головы”**.  
Они формируются **только из элементов Document Plan**:

- `RequiredField` со статусом `missing`,
- `DecisionPoint` со статусом `missing`,
- `RequiredField` со статусом `uncertain`.

Если элемент схемы заполнен — вопрос по нему **не задаётся**.

---

### 4.2. Приоритет вопросов

В каждый момент времени выбирается **один следующий вопрос** по приоритету:

1. Обязательные `RequiredField` со статусом `missing`
2. Обязательные `DecisionPoint` со статусом `missing`
3. Поля со статусом `uncertain`
4. Опциональные пакеты (в зависимости от уровня рассуждения)

Это исключает “пулемёт вопросов” и создаёт ощущение логичного диалога.

---

### 4.3. Формирование вопроса

Для каждого элемента схемы формируется `UserQuestion`:

- тип вопроса (`single_choice`, `multi_choice`, `free_text`, `form`),
- краткий заголовок,
- пояснение, зачем нужен ответ,
- варианты выбора (если применимо),
- указание, к какому месту документа относится вопрос.

Формулировка вопроса может:
- генерироваться с помощью LLM,
- но строго в рамках заданной схемы и структуры.

LLM отвечает на вопрос **«как корректно и понятно спросить»**, а не **«о чём спросить»**.

---

## 5. Интеграция с HITL и UI

### 5.1. HITL как часть пайплайна

Если следующий шаг пайплайна требует ответа пользователя:
- агент возвращает `need_user_input`,
- выполнение пайплайна приостанавливается,
- ожидается ответ пользователя.

Без ответа по обязательному элементу схема считается незавершённой, и генерация документа не продолжается.

---

### 5.2. Связь вопроса с документом

Каждый вопрос:
- связан с узлом схемы,
- а через него — с конкретной секцией или пунктом документа.

UI:
- подсвечивает соответствующий фрагмент документа слева,
- показывает вопрос и варианты ответа справа в чате.

---

## 6. Обновление схемы

После ответа пользователя:
- соответствующий `RequiredField` или `DecisionPoint` помечается как `present` или `decided`,
- схема обновляется,
- выбирается следующий вопрос или продолжается генерация.

Схема может пересобираться:
- при смене уровня рассуждения,
- при значительных ручных правках документа,
- при обнаружении конфликтов на этапе линтинга.

---

## 7. Результат подхода

Этот подход обеспечивает:

- адаптивные вопросы под конкретный договор,
- отсутствие нерелевантных и повторяющихся вопросов,
- предсказуемый и контролируемый UX,
- возможность строгой валидации,
- масштабируемость под любые типы и гибриды документов.

Юридический агент перестаёт быть “опросником”  
и становится **проводником по логике конкретного договора**.

```

---

