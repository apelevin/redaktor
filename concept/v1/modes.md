Да, твоя идея с режимами — прям напрашивается, учитывая, что сейчас пайплайн почти всегда едет в “максималку” и рожает монструозный договор типа того Range Rover-а 😄

Ниже — доработанная концепция архитектуры **с режимами генерации договора**, опираясь на текущие шаги 3–4 и промпты.
Ты можешь это почти целиком закинуть в Cursor как черновое ТЗ.

---

## 1. Вводим понятие «режим договора»

### 1.1. Новое поле в store

В `document-store` добавить:

```ts
documentMode: 'short' | 'standard' | 'extended' | 'expert';
```

* `short` — краткий, практичный договор без лишней “юридистики” (режим по умолчанию).
* `standard` — обычный подробный договор (то, что большинство юристов считают “нормой”).
* `extended` — максимум разделов и пунктов, но без сверх-расширания текста.
* `expert` — гипердетализированный договор для сложных сделок (тот, что ты сейчас получил).

`documentMode` должен храниться в одном месте и **протаскиваться через все шаги**: skeleton, вопросы, генерация текста.

---

## 2. Где пользователь выбирает режим

### 2.1. Место в UI

Самый логичный вариант:

* В начале **шага 3** (Step3Panel) — перед или рядом с кнопкой “Сгенерировать структуру документа” добавить селектор:

> “Режим договора:
> – Краткий (по умолчанию)
> – Стандартный
> – Расширенный
> – Экспертный”

При выборе:

* Обновляется `documentMode` в store.
* Всё остальное (skeleton, вопросы, генерация текста) уже учитывает этот режим.

---

## 3. Как режим влияет на шаг 3 (скелет + вопросы)

### 3.1. Генерация скелета (`/api/pipeline/skeleton`)

Сейчас endpoint принимает `document_type`, `generated_context`, `jurisdiction`, `style`.

**Добавляем:**

```json
{
  "document_type": "...",
  "generated_context": "...",
  "document_mode": "short" | "standard" | "extended" | "expert",
  ...
}
```

И в промпт `skeleton-generation.md` добавляем явный блок:

> **Параметр детализации:** `{{document_mode}}`
>
> * Если режим = `short` —
>   создай **компактный скелет**:
>
>   * минимально необходимый набор разделов и пунктов;
>   * не дроби одну тему на 3–4 пункта, если можно обойтись одним;
>   * убирай “тонкие” корпоративные блоки, если они не критичны.
> * Если режим = `standard` —
>   типовой договор: полная структура, но без чрезмерной детализации.
> * Если режим = `extended` —
>   добавь дополнительные пункты для рисков, гарантий, ответственности, но не превращай каждый микросюжет в отдельный пункт.
> * Если режим = `expert` —
>   максимально подробный юридический каркас, включая расширенные заверения, детальную ответственность, форс-мажор, расширенную претензионку и т.п.

Это даст LLM сигнал, **сколько именно секций и пунктов создавать**, а не всегда стрелять в “enterprise-контракт”.

### 3.2. Выбор пунктов скелета (Step3Panel)

Здесь режим можно использовать так:

* `short`:
  – по умолчанию **предвыбраны только ключевые пункты** (предмет, стороны, цена, сроки, ответственность, споры, подписи);
  – всё второстепенное **снято**, пользователь может включить сам.

* `standard`:
  – большинство пунктов выбрано по умолчанию, кроме совсем экзотики.

* `extended`/`expert`:
  – всё выбрано по умолчанию.

Это позволит без изменения скелета уже сильно сокращать будущий текст.

### 3.3. Генерация уточняющих вопросов (`/api/pipeline/skeleton-item-question`)

Сейчас логика: один пункт → максимум один вопрос, и вопрос не задаётся, если всё уже есть в `generated_context` или `existing_answers`.

**Добавляем в запрос и промпт:**

```json
{
  "document_type": "...",
  "generated_context": "...",
  "document_mode": "...",
  ...
}
```

И в промпте дописываем:

> * В режиме `short` задавай вопросы только по **критичным** параметрам (существенные условия + ключевые риски). Если пункт можно разумно заполнить типовой формулировкой — **не задавай вопрос**.
> * В режиме `standard` — как сейчас.
> * В режиме `extended` — можно задавать больше уточняющих вопросов, если это реально добавляет юридическую ценность.
> * В режиме `expert` — допустимо задавать уточняющий вопрос для большинства пунктов, если от этого появятся более точные формулировки.

То есть на режим `short` ты накладываешь ещё один фильтр “даже если вопрос возможен, он не обязателен — не задавай”.

---

## 4. Как режим влияет на шаг 4 (генерация текста по пунктам)

Сейчас `document-item-generation` получает `style`, `generated_context`, `item_text`, ответы и existing_clauses.

### 4.1. Передача режима

В `POST /api/pipeline/document-item-generation` добавляем:

```json
{
  "document_type": "...",
  "generated_context": "...",
  "document_mode": "short" | "standard" | "extended" | "expert",
  ...
}
```

И протаскиваем `document_mode` в `generateDocumentItem()` и дальше в промпт.

### 4.2. Изменения в промпте `document-item-generation.md`

Сейчас он просит “полный, юридически корректный текст” без ограничений по объёму.

Добавляем блок:

> **Параметр детализации:** `{{document_mode}}`
>
> * Если `document_mode = "short"`:
>
>   * пиши **кратко и ёмко**;
>   * избегай повторения одной и той же мысли разными абзацами;
>   * не расписывай стандартные юридические оговорки (форс-мажор, общие нормы ответственности) длинными блоками — достаточно 1–2 плотных абзацев;
>   * не дублируй условия, уже отражённые в других пунктах.
> * Если `standard` — обычная детализированная формулировка, но без излишней “литературщины”.
> * Если `extended` — можно подробнее раскрывать последствия, процедуры, варианты, но **старайся избегать повторов**.
> * Если `expert` — допускается максимальная детализация и юридический “boilerplate” уровня B2B.

И отдельное правило:

> При режиме `short` ориентируйся на:
> – 1 абзац или 3–6 насыщенных предложений на пункт.
> При режиме `standard` — 1–2 абзаца.
> При `extended` и `expert` ограничений меньше, но **не повторяй один и тот же смысл в разных формулировках**.

Этим ты прямо задаёшь модели “крышку кастрюли” по длине и стилю.

---

## 5. Дополнительные архитектурные улучшения вокруг режимов

### 5.1. “Вес” пунктов скелета

Имеет смысл в `Section.items` или в отдельной структуре завести **важность пункта**:

```ts
interface SkeletonItem {
  id: string;
  text: string;
  importance: 'core' | 'normal' | 'advanced';
}
```

Тогда:

* В `short` по умолчанию выбираем только `core`;
* `standard` — `core + normal`;
* `extended/expert` — всё.

И `skeleton-item-question` может использовать `importance` + `document_mode`, чтобы решать, стоит ли вообще грузить пользователя вопросами по этому пункту.

### 5.2. Возможность поменять режим перед генерацией текста

Ты можешь позволить:

* пройти шаги 2–3 в режиме `extended`,
* а перед шагом 4 дать пользователю переключатель:

> “Сгенерировать текст в режиме: Краткий / Стандартный / Расширенный”

Технически:

* `documentMode` сохраняется как “предпочтение по структуре”,
* а перед шагом 4 можно иметь отдельный `outputTextMode`, который влияет только на длину/стиль текста.

Это позволит:

* иметь полный скелет,
* но по факту наполнять его **короткими** формулировками (как ты сейчас хочешь).

---

## 6. Что ты выигрываешь этой архитектурой

1. **Управляемая длина и сложность договора**
   Пользователь сразу понимает: “кратко/стандартно/очень подробно”.

2. **Гибкость без усложнения пайплайна**
   Ты не меняешь фундамент архитектуры шагов 3–4, а всего лишь добавляешь один параметр, который идёт через skeleton → questions → clauses.

3. **Снижение риска “монстро-договоров” по умолчанию**
   Default = `short`, а для “юрист-режима” надо осознанно выбрать `extended` / `expert`.
