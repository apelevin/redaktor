Ниже — **единое, полностью универсальное ТЗ + концепция**, не привязанная ни к одному типу договора.
Текст написан так, чтобы ты мог **прямо вставить в Cursor** как основу для реализации шага 2 (генерация скелета документа).

---

# **🧩 Техническое задание: Шаг 2 — Генерация Скелета Договора**

## **1. Цель шага**

На этом шаге система должна сформировать **полный скелет будущего договора** — структуру документа, состоящую из разделов и пунктов, **без юридических формулировок**, только логическая последовательность.

Скелет должен:

* отражать **минимально необходимые разделы и пункты**, которые обычно присутствуют в юридических документах данного типа (в рамках российской юрисдикции);
* учитывать **ответы пользователя**, полученные на шаге уточняющих вопросов;
* **дополнять недостающие разделы**, даже если про них не было вопросов;
* корректно обозначать разделы, по которым пользователь заявил "достаточно общих норм", но не исключать их;
* сохранять **юридическую полноту**, а не просто отображать заполненный контекст.

---

# **2. Важный принцип**

Контекст пользователя:

* **отражает только часть договора**,
* **не является полным описанием структуры**,
* служит **подсказкой для адаптации типовой структуры**,
* **не ограничивает состав разделов**.

LLM обязан создать **полный, юридически корректный каркас**, включая разделы, которые не были обсуждены в диалоге.

---

# **3. Основная логика формирования скелета**

Для каждого документа LLM выполняет следующий алгоритм:

### **Шаг 1. Определить набор стандартных разделов для договора данного типа**

LLM должен:

* восстановить типовую структуру договора для данного юридического сценария (например, аренда, услуги, NDA, поставка и т.д.);
* сформировать **полный список ключевых разделов**, который обычно применяется в Российской Федерации.

Важно:
LLM делает это **самостоятельно**, опираясь на юридическую практику, а не на заранее зашитые шаблоны.

---

### **Шаг 2. Адаптировать разделы под контекст**

Каждый раздел проходит оценку:

1. **Есть ли в контексте конкретные специальные условия?**
   → создать соответствующий пункт внутри раздела.

2. **Пользователь явно указал “нет, достаточно общих норм”?**
   → раздел остаётся в структуре, но внутри будет пункт типа:

   * “Применяются общие нормы закона, без специальных условий”.

3. **Контекст ничего не сказал о разделе?**
   → раздел включается как типовой, а его пункты будут наполнены на шаге 3 стандартными формулировками.

Нельзя исключать раздел только потому, что про него не спрашивали.

---

### **Шаг 3. Обеспечить юридическую последовательность**

Разделы должны идти в логическом и юридическом порядке:

1. Стороны
2. Предмет / объект договора
3. Срок
4. Условия оплаты / расчёты
5. Обеспечение исполнения
6. Передача / возврат
7. Обязанности сторон
8. Ограничения и правила пользования
9. Ответственность
10. Персональные данные (если применимо)
11. Форс-мажор
12. Расторжение
13. Разрешение споров
14. Применимое право
15. Подписи
16. Прочие условия

Это ориентировочный порядок — LLM должен выстроить структуру так, как принято в юридической практике.

---

### **Шаг 4. Сформировать точки будущего наполнения**

Каждый пункт должен быть “контейнером”, в который на шаге 3 будет вставлен текст юридической формулировки.
Поэтому пункты должны быть:

* краткими;
* описательными;
* уникальными;
* не содержать юридического текста;
* отражать содержание, которое должно быть раскрыто далее.

Пример:

```
"items": [
  "Идентификация сторон",
  "Описание объекта договора",
  "Порядок внесения платежей",
  "Последствия просрочки платежа",
  "Условия прекращения договора"
]
```

---

# **4. Формат выходных данных (универсальный JSON)**

Скелет документа должен возвращаться в следующем формате:

```json
{
  "documentType": "<тип_документа>",
  "sections": [
    {
      "id": "<уникальный_id_раздела>",
      "title": "<название раздела>",
      "items": [
        "<пункт 1>",
        "<пункт 2>",
        "<пункт 3>"
      ]
    }
  ]
}
```

Требования:

* `id` — машинное имя (snake_case).
* `title` — человекочитаемое название раздела.
* `items` — массив названий пунктов (только заголовки, без текста).
* Пункты должны соответствовать контексту, но также включать недостающие элементы договора.

---

# **5. Правила взаимоотношения контекста и структуры**

Вставь это в Cursor чтобы зафиксировать поведение:

## **5.1. Контекст — не ограничитель**

Даже если контекст ничего не сказал про “Форс-мажор”, “Порядок разрешения споров”, “Конфиденциальность”, “Подписание” —
**эти разделы должны появиться в скелете**, потому что они входят в юридическую структуру документа.

## **5.2. Контекст — модификатор**

Если контекст уточнил конкретный пункт — скелет должен это отразить.

Если контекст указал “не включать ничего специального”, то:

* раздел остаётся,
* но внутри — минимальный пункт про “общие нормы закона”.

## **5.3. Контекст — источник вариативности**

Пример: если пользователь выбрал “бессрочный договор”, LLM заменяет пункт
“Срок аренды” на
“Бессрочный характер договора + порядок расторжения”.

---

# **6. Требования к LLM при генерации скелета**

LLM должен:

* понимать юридическую структуру документа;
* уметь достраивать недостающие разделы;
* использовать контекст только для уточнения, но не для ограничивания;
* не возвращать формулировки текста;
* не сокращать структуру, даже если пользователь дал мало данных;
* выводить JSON строго по указанной схеме.

---

# **7. Короткая инструкция для Cursor**

Вставь этот блок как системное правило:

> **LLM должен формировать полный скелет договора, не ограничиваясь контекстом пользователя.
> Контекст модифицирует структуру, но не определяет её.
> Все стандартные разделы договора (по российской юридической практике) должны присутствовать, даже если в контексте нет данных по ним.
> Каждая тема должна быть представлена в виде пункта-держателя, в который на следующем шаге будет вставлен юридический текст.
> На выходе должен быть строго структурированный JSON из разделов и пунктов без текста формулировок.**

---

