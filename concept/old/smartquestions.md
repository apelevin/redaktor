

# 📘 **CONCEPT: Уточняющие вопросы с самообучением (Questionnaire Engine)**

Этот документ описывает систему уточняющих вопросов для подготовки контекста перед генерацией skeleton-а документа. Система работает в двух режимах:

1️⃣ **Cold Start** — когда для данного типа документа ещё нет инструкции в Pinecone.
2️⃣ **Warm Start** — когда инструкция уже есть и содержит сохранённый “опросник” (questionnaire).

Система поддерживает **три типа вопросов**:

* `open` — свободный текст,
* `single_choice` — выбор одного варианта,
* `multi_choice` — множественный выбор из предложенных вариантов.

Цель — сделать вопросы максимально простыми для пользователя (кнопки там, где возможно) и самообучаемыми (сохранять лучшие вопросы, чтобы больше не генерировать их LLM-ом).

---

# 1. Общая архитектура

Процесс уточняющих вопросов работает до генерации skeleton и служит для сбора минимально достаточного юридического контекста через диалог “вопрос-ответ”.

Внутренний цикл:

```
→ определить, какие поля документа ещё не покрыты
→ задать один вопрос (open / single / multi)
→ получить ответ и сохранить его в qa_context
→ повторить цикл
→ как только критично важные поля покрыты → завершить и перейти к skeleton
```

Вопросы задаются строго **по одному**, чтобы пользователь не перегружался.

---

# 2. Два режима работы

## 2.1. Warm Start (опросник уже существует)

Если в Pinecone (index: `instructions`) находится инструкция по `document_type` **и в ней есть поле `questionnaire`**, то:

* система **не вызывает LLM** для генерации вопросов;
* вопросы задаются последовательно из заранее сохранённых `QuestionTemplate`;
* UI рендерит:

  * кнопки (single_choice),
  * галочки (multi_choice),
  * текстовое поле (open);
* завершение наступает, когда все `required: true` вопросы имеют ответы.

Это обеспечивает:

* стабильность сценариев,
* скорость,
* предсказуемость.

---

## 2.2. Cold Start (опросника нет → формируем новый)

Если инструкция в Pinecone не найдена или в ней нет `questionnaire`, запускается режим cold-start:

1. Система определяет, какие смысловые поля документа ещё не покрыты (`computeMissingFields`).
2. LLM генерирует **один вопрос** в формате JSON (см. структуру ниже).
3. Система показывает этот вопрос пользователю.
4. Ответ сохраняется.
5. Цикл повторяется, пока не собрана минимальная информация.
6. После окончания:

   * все сгенерированные вопросы → сохраняются как `questionnaire` в инструкции Pinecone;
   * далее для этого типа документа всегда будет warm-start.

Таким образом опросник **автоматически обучается на первом пользователе**.

---

# 3. Структура вопроса (QuestionTemplate)

Каждый вопрос — строгая структура:

```ts
export type QuestionKind = 'open' | 'single_choice' | 'multi_choice';

export interface QuestionTemplate {
  id: string;              // стабильный смысловой идентификатор (snake_case)
  text: string;            // формулировка вопроса
  kind: QuestionKind;      // open | single_choice | multi_choice
  options?: string[];      // варианты (если single/multi)
  required: boolean;       // нужно ли спрашивать всегда
  order: number;           // порядок задавания в опроснике
  minSelections?: number;  // только для multi_choice
  maxSelections?: number;  // только для multi_choice
}
```

---

# 4. Хранение в Pinecone

В index `instructions` каждая инструкция хранит:

```json
{
  "document_type": "договор поставки",
  "style": "строгий",
  "skeleton_id": "skl_123",
  "questionnaire": [
    {
      "id": "parties",
      "text": "Кто является поставщиком и покупателем?",
      "kind": "open",
      "required": true,
      "order": 1
    },
    {
      "id": "price_model",
      "text": "Какую модель ценообразования вы используете?",
      "kind": "single_choice",
      "options": [
        "Фиксированная цена",
        "Диапазон",
        "Цена по формуле",
        "Пока не знаю"
      ],
      "required": true,
      "order": 2
    },
    {
      "id": "delivery_type",
      "text": "Каким образом будет осуществляться поставка?",
      "kind": "multi_choice",
      "options": [
        "Разовая поставка",
        "Периодическая поставка",
        "По заявкам",
        "По графику"
      ],
      "minSelections": 1,
      "maxSelections": 3,
      "required": false,
      "order": 3
    }
  ]
}
```

Этот блок (`questionnaire`) полностью заменяет вопросогенерацию LLM в warm-start.

---

# 5. Алгоритм выбора следующего вопроса

1. Вывести список `required` полей, на которые нет ответов.
2. Если такие вопросы есть → задаём следующий по order.
3. Если требуемые вопросы закончились → можно задавать опциональные (если хочешь расширить контекст).
4. Если все `required` покрыты → завершить цикл.

В cold-start:

* вопрос для следующего поля генерирует LLM,
* LLM сам выбирает format (open / single_choice / multi_choice),
* мы сохраняем вопрос в локальный массив `questionTemplates`,
* после пайплайна этот массив упаковывается в инструкцию и сохраняется в Pinecone.

---

# 6. Формат ответа LLM в cold-start

Используется `response_format: json_object`.
Ожидаем JSON:

```json
{
  "id": "price_model",
  "text": "Какую модель ценообразования вы планируете использовать?",
  "kind": "single_choice",
  "options": [
    "Фиксированная цена",
    "Диапазон",
    "Цена по формуле",
    "Пока не знаю"
  ],
  "required": true,
  "order": 2
}
```

Правила для модели:

* Если возможен набор вариантов → `single_choice` или `multi_choice`.
* Если вариантов быть не может → `open`.
* Всегда возвращать валидный JSON.
* `id` должен быть смысловым (snake_case).
* `order` — порядковый номер вопроса.

---

# 7. UI-логика (фронтенд)

Рендеринг вопроса зависит от его `kind`:

### ✔ open

Поле ввода текста + кнопка отправки.

### ✔ single_choice

Кнопки/радио-группа.
При клике ответ отправляется сразу.

### ✔ multi_choice

Чекбоксы.
Кнопка “Подтвердить выбор”, отправляет массив строк.

---

# 8. Сохранение ответов

Тип ответа:

```ts
export type AnswerValue = string | string[];
```

Запись в `qa_context`:

```ts
{
  questionId: template.id,
  question: template.text,
  answer: AnswerValue
}
```

---

# 9. Логика завершения (is_complete)

Условия:

1. Все вопросы с `required: true` из опросника имеют ответы → контекст собран.
2. В cold-start, если достигнут разумный лимит (например 6–8 вопросов), а критических дыр нет → завершить.
3. В warm-start LLM не вызывается — завершение чисто по наличию ответов.

---

# 10. Как происходит самообучение

## 10.1. Первый раз (cold-start)

1. LLM генерирует вопросы по одному.
2. Каждый вопрос сохраняем в `questionTemplates[]`.
3. После завершения пайплайна:

   * skeleton + questionnaire сохраняются в Pinecone как новая инструкция.

## 10.2. Второй и последующие разы (warm-start)

1. Найдена инструкция → достаём готовый questionnaire.
2. Задаём вопросы строго по шаблону → без LLM.
3. Процесс очень быстрый.
4. Если skeleton/вопросы меняются со временем → можно сохранять новые версии в Pinecone.

---

# 11. Встраивание в существующий пайплайн

Шаг "Сбор контекста" теперь:

### Warm-start:

```
load questionnaire → ask question → save answer → repeat → complete → generate skeleton
```

### Cold-start:

```
compute missing fields → ask LLM for a QuestionTemplate → save template & ask user → repeat → 
save questionnaire to Pinecone → generate skeleton
```

Дальше пайплайн идёт стандартно:

* поиск клауз в RAG,
* генерация клауз,
* когерентность,
* сборка документа,
* сохранение в Pinecone.

---

# 12. Суть концепции в одном предложении

**Система учится у самого первого пользователя: LLM создаёт вопросы, а все следующие пользователи получают уже готовый, оптимизированный и кликабельный опросник как часть инструкции в Pinecone.**

---
