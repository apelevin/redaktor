

# 🟧 Часть 1. Идеальный промпт для генерации инструкции LLM

Этот промпт гарантирует:

* отсутствие персональных данных;
* только плейсхолдеры;
* обобщённость;
* корректную юридическую структуру;
* единый формат.

---

## **PROMPT: GenerateInstructionFromDocument**

**SYSTEM PROMPT**

Ты — юридический ассистент уровня senior, специализирующийся на договорных конструкциях и анализе юридических документов.
Твоя задача — преобразовать конкретный договор в универсальную, обобщённую, полностью обезличенную **инструкцию**, которая описывает:

1. когда используется документ этого типа;
2. какие данные нужно спросить у пользователя;
3. какая структура документа рекомендуется;
4. какие формулировочные особенности важно соблюдать.

Требования:

* НЕ включать конкретные имена, марки, VIN, реквизиты, суммы, адреса, даты.
* Использовать только заранее переданные плейсхолдеры (`{CONTRACT_...}`, `{OBJECT_...}`, `{PARTY_...}`, `{AMOUNT}`, `{DATE}`, `{VIN}`, `{ADDRESS}`, `{VALUE_...}` и др.).
* Отражать только **обобщённую юридическую логику**, подходящую для множества будущих документов.
* Не создавать новые плейсхолдеры — использовать переданные.
* Не описывать конкретный автомобиль/товар/услугу, только правила того, что нужно указать.
* Не копировать текст договора — только смысл.

Выводи результат строго в заданной структуре.

---

**USER PROMPT**

Входные данные:

1. **DocumentType:** `{documentType}`
2. **Jurisdiction:** `{jurisdiction}`
3. **AnonymizedDocument (обезличенный текст документа):**

```
{sanitizedDocument}
```

4. **Skeleton (структура документа):**

```
{skeleton}
```

5. **UserQuestions (вопросы, которые задавались пользователю):**

```
{questions}
```

Сгенерируй **универсальную инструкцию** строго по следующему формату:

```
{
  "documentType": "...",
  "jurisdiction": "...",
  "whenToUse": "...",
  "requiredUserInputs": [...],
  "recommendedStructure": [
    { "sectionKey": "...", "title": "...", "description": "...", "isMandatory": true | false }
  ],
  "styleHints": {
    "tone": "neutral_business",
    "riskProfile": "balanced | conservative | aggressive",
    "mustHaveSections": [...],
    "notes": [...]
  },
  "placeholdersUsed": [...],
  "instructionQuality": "high | medium | low"
}
```

Особые требования:

* `whenToUse` — 2–5 предложений общего описания ситуации, в которой применяется документ.
* `requiredUserInputs` — только уникальные вопросы, сгруппированные по смыслу.
* `recommendedStructure` — не копия исходного скелета; это обобщённая, оптимальная структура для документа данного типа.
* `description` — короткая суть каждого раздела, без упоминания конкретных данных.
* `isMandatory` — true, если без раздела документ юридически неполноценен.
* `placeholdersUsed` — список всех плейсхолдеров, найденных во входе.
* `instructionQuality` — твоя оценка того, насколько качественная получилась инструкция.

Выведи строго JSON.

---

# 🟧 Часть 2. Улучшенный формат инструкции для хранения в Pinecone

Это **идеальный формат**, оптимизированный для:

* поиска по смыслу (основной текст для embedding);
* лаконичности;
* стабильности структуры;
* лёгкости извлечения;
* RAG-пайплайна.

---

## **Pinecone Instruction Record**

### 🔹 `id`

Строка, генерируемая UUID, например:
`"inst_15cd57f0-60bf-479c-9fb8-e8b57513c758"`

### 🔹 `values`

Embedding создаётся на основе текстовой репрезентации:

```
{documentType}. {jurisdiction}. {whenToUse}. {titles of recommendedStructure}
```

### 🔹 `metadata`

```
{
  "documentType": "car_sale_agreement",
  "jurisdiction": "RU",
  "whenToUse": "...",
  
  "requiredUserInputs": [
    "seller identification",
    "buyer identification",
    "object description",
    "price and payment method",
    "encumbrances"
  ],

  "recommendedStructure": [
    {
      "sectionKey": "parties",
      "title": "Стороны договора",
      "description": "Кто участвует в сделке и в каких ролях.",
      "isMandatory": true
    },
    {
      "sectionKey": "subject",
      "title": "Предмет договора",
      "description": "Что продаётся и какие характеристики необходимо указать.",
      "isMandatory": true
    },
    ...
  ],

  "styleHints": {
    "tone": "neutral_business",
    "riskProfile": "balanced",
    "mustHaveSections": ["parties", "subject", "price", "transfer", "liability"],
    "notes": [
      "Необходимо явно фиксировать отсутствие или наличие обременений.",
      "Важно описать момент перехода права собственности и рисков."
    ]
  },

  "placeholdersUsed": [
    "{PARTY_1}", "{PARTY_2}", "{OBJECT_АВТОМОБИЛЬ}", "{AMOUNT}", "{CONTRACT_ДОГОВОР}"
  ],

  "createdAt": "2025-12-08T06:21:00.000Z",
  "isSavedToKnowledgeBase": true
}
```

---

# 🟧 Часть 3. Полный pipeline задачи от начала до конца

Ниже — единая спецификация всех шагов, которую можно отдавать в разработку как техническое задание.

---

# 🔵 **Шаг 1. Генерация финального документа (как сейчас)**

Пайплайн формирует:

* финальный текст документа;
* скелет (список разделов);
* вопросы → ответы пользователя;
* метаданные.

---

# 🔵 **Шаг 2. Анонимизация**

Перед созданием инструкции необходимо:

1. Выделить все персональные и конкретные сущности:

   * ФИО
   * VIN, госномера
   * адреса
   * суммы, даты
   * реквизиты
   * марка/модель автомобиля
2. Заменить их на плейсхолдеры, используя заранее утверждённый словарь.

Выход: **sanitizedDocument**.

---

# 🔵 **Шаг 3. Генерация инструкции (LLM)**

Вызов LLM с промптом **GenerateInstructionFromDocument** + данные:

* sanitizedDocument
* skeleton
* userQuestions
* documentType
* jurisdiction

LLM возвращает:

* корректный JSON
* строгий формат
* без личных данных
* с обобщённой структурой
* готовый к помещению в Pinecone

---

# 🔵 **Шаг 4. Создание Pinecone-записи**

1. Формируется текст для embedding:

```
"{documentType}. {jurisdiction}. {whenToUse}. {structureTitles}"
```

2. Записывается в индекс:

```
index.upsert({
  id,
  values: embedding,
  metadata: instructionJSON
})
```

---

# 🔵 **Шаг 5. Отображение инструкции в UI**

UI просто отображает JSON человеческим языком (как ты уже делаешь), не влияя на структуру.

---

# 🔵 **Шаг 6. Использование инструкции через RAG**

Перед генерацией нового документа:

1. Создаётся embedding из:

```
"описание документа + тип + юрисдикция + несколько ключевых атрибутов"
```

2. Выполняется поиск в Pinecone по instructions:

```
topK=3  
filter: { documentType: <>, jurisdiction: "RU" }
```

3. Находится лучшая инструкция → используется:

   * для формирования скелета;
   * для выбора вопросов;
   * для выбора формулировок (через второй индекс clauses).

---

# 🟧 Часть 4. Связанный комплект задач (как выдавать в Cursor)

Ниже — пакет задач **в хронологическом порядке**, готовый к отправке в Cursor.

---

## **Task 1: Implement Document Sanitization**

Создать модуль, заменяющий персональные и конкретные данные документа на плейсхолдеры.

---

## **Task 2: Implement Instruction LLM Generator**

Сделать функцию:

```
generateInstruction({
  sanitizedDocument,
  skeleton,
  questions,
  documentType,
  jurisdiction
})
```

использующую промпт **GenerateInstructionFromDocument**
и возвращающую JSON-инструкцию.

---

## **Task 3: Implement Instruction Storage in Pinecone**

Функция:

```
saveInstructionToPinecone(instruction)
```

* генерирует embedding
* помещает в `instructions`
* сохраняет `metadata` = JSON инструкции.

---

## **Task 4: Implement Instruction Retrieval for UI**

API:

```
GET /api/document-sessions/{id}/instruction
```

Возвращает JSON-инструкцию.

---

## **Task 5: Update UI — add “Instruction” tab**

Создать вкладку, которая отображает инструкцию, не изменяя её структуру.

---
